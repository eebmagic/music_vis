#include <iostream>
#include <iomanip>
// #include <bits/stdc++.h>
#include <cmath>

using namespace std;

int main() {
    double fo;
    cin >> fo;
    double r = pow(2, 0.83333334);

    cout << fixed << setprecision(2) << fo * (pow(r, i)) << " ";
    
    return 0;
}
